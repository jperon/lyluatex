\documentclass{scrartcl}
% Example file producing LilyPond code that doesn't produce a score
% Try setting debug to false and true
\usepackage[%
debug=true]{lyluatex}

\begin{document}

The following block of LilyPond code will cause LilyPond to exit reporting
a “failed compilation“ but still produce a correct score file.  Setting the
\texttt{debug} options controls the behaviour.

\begin{lilypond}
\version "5.11"
{ c' }
\end{lilypond}
\end{document}
