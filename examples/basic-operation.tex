\documentclass{scrartcl}
\usepackage{lyluatex}
\usepackage{lyluatexexamples}

\begin{document}

\section*{\lyluatex\ -- Basic Operation}

Include a score in systems-by-systems mode (default for included files and
environment) using \cmd{begin\{lilypond\}}:

\begin{lilypond}[]
c' d' e' f'
\end{lilypond}


\bigskip
Include a score using the \cmd{lilypond} command:\lilypond{c' d' e' f'}%
Note that the score is inserted inline within the paragraph and the staff size
is automatically scaled to 2/3 of the regular scores.

\bigskip
Include an external file with \cmd{lilypondfile}

\bigskip
\lilypondfile{../ly/Sphinx/Sphinx}


\end{document}
